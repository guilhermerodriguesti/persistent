function processData() {    const inputData = document.getElementById('inputData').value;    const resultDiv = document.getElementById('result');    // Retirar espaços desnecessários    let cleanedData = inputData.replace(/\s+/g, ' ').trim();    // Quebrar em linhas e colunas    const lines = cleanedData.split(' / ');    const rows = lines.map(line => line.split(', '));    // Ordenar os dados pelo nome do candidato (índice 1)    rows.sort((a, b) => a[1].localeCompare(b[1]));    // Gerar a tabela de resultados    let tableHTML = '<table>';    tableHTML += `        <tr>            <th>Inscrição</th>            <th>Nome</th>            <th>Nota P1</th>            <th>Acertos P1</th>            <th>Nota P2</th>            <th>Acertos P2</th>            <th>Nota Final Objetiva</th>            <th>Nota Provisória Q1 P3</th>            <th>Nota Provisória Q2 P3</th>            <th>Nota Provisória Discursiva</th>        </tr>    `;    rows.forEach(row => {        tableHTML += '<tr>';        row.forEach(cell => {            tableHTML += `<td>${cell}</td>`;        });        tableHTML += '</tr>';    });    tableHTML += '</table>';    resultDiv.innerHTML = tableHTML;}